# Providers use lazy imports — each client imports its SDK only when instantiated.
# Do NOT add eager imports here: each agent venv may not have all SDKs installed.
# Import directly: from shared.llm.providers.qwen_client import QwenClient

