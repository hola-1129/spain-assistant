# Codex Workspace

This directory is the Codex-specific workspace under the shared
`/Volumes/AI_DISK/ai_workspace` tree.

Use this path when starting Codex sessions that should follow the local
`AGENTS.md` rules without changing the shared root workspace used by Claude.

Shared project assets remain in the parent workspace. Codex work should keep
changes scoped, minimal, observable, and reversible.

## Agent Links

The agent directories below are linked into this Codex workspace for easier
navigation while preserving their canonical location under
`/Volumes/AI_DISK/ai_workspace/agents`.

- `_template` -> shared agent template
- `ai_narrative_rotation_radar`
- `finance_bot`
- `polymarket_intelligence`
- `school_helper`
- `travel_assistant`
- `web3_monitor`
- `web3_monitor_v0_backup`

Canonical parent directory:
`/Volumes/AI_DISK/ai_workspace/agents`
